function getNumber(a = 100, b = 0){
    return Math.floor(Math.random() * (a-b)+b)
}

function getColor(n){
    // let r = getNumber(256), 
    // g = getNumber(256), 
    // b = getNumber(256);
    if(n >= 50){
    return `orange`
    }
}